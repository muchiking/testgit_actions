name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    outputs:
      output1: $
      output2: $
    steps:
      # The shell and env declaration needs to be associated with a step
      # It's better to define environment variables directly within a 'step' or 'job' block.
      # If you want it for a specific step, put it under that step.
      # If you want it for all steps in a job, put it under the 'job' level, like this:
      # env:
      #   SEC: ${{ secrets.SEC }}
      # For demonstration, I'll place it directly where you tried to use it.

      - name: Set secret as environment variable
        # run: echo "Setting secret..."
        env:
          SEC: ${{ secrets.SEC1 }} # Corrected line
          VAR1: ${{ vars.VAR1 }}
        shell: bash # You can define shell for a specific step if needed

        # if: ${{VAR1 == 'varonevalue' }} 
        if: ${{vars.VAR1 == 'varonevalue' }} 
        run: echo  "The secret value is $SEC  $VAR1" 
        # run: echo "the variable value is"

      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      # - name: Echo the secret
        # run: echo "The secret value is: $SEC" # Accessing the environment variable
        # If you were to directly echo the secret (not recommended due to logs), it would be:
        # - run: echo "${{ secrets.SEC }}"
      - id: step1
        run: echo "test=hello" >> "$GITHUB_OUTPUT"
      - id: step2
        run: echo "test=world" >> "$GITHUB_OUTPUT"
  job1:
    runs-on: ubuntu-latest
    needs: Explore-GitHub-Actions
    steps:
      - name: configure the system
        env:
            OUTPUT1: $
            OUTPUT2: $
        run: echo "$OUTPUT1 $OUTPUT2"